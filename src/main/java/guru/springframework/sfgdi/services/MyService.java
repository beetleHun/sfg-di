package guru.springframework.sfgdi.services;

/**
 * This interface is implemented only by the {@link MyServiceImpl} class, thus it's ok to call it simply
 * by the name of the interface + impl postfix
 */
public interface MyService {
}
